/**
 * 
 */
$( document ).ready(function() {
	//hide login, data
	
	//TODO: Check if we are logged in (session, cookie)
		//if not -> display login
		//else -> display data
	
	//handler: login click
		//checkLogin.php <- check return, display content, hide login
	
	//handler: button click 
		//writeData.php
	
});